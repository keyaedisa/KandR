#include <stdio.h>
#include <limits.h>
#include <float.h>

//skipped this too, variables have limits. understood
//also this like idk very hard for no reason so im moving on

int main(){
    char charMax, charMin;
    signed char charMaxSigned, charMinSigned;
    unsigned char charMaxUnsigned, charMinUnsigned;
    short shortMax, shortMin;
    signed short shortMaxSigned, shortMinSigned;
    unsigned short shortMaxUnsigned, shortMinUnsigned;
    long longMax, longMin;
    signed long longMaxSigned, longMinSigned;
    unsigned long longMaxUnsigned, longMinUnsigned;
    int intMax, intMin;
    signed int intMaxSigned, intMinSigned;
    unsigned int intMaxUnsigned, intMinUnsigned;

    charMax = CHAR_MAX;
    charMin = CHAR_MIN;
    charMaxSigned = SCHAR_MAX;
    charMinSigned = SCHAR_MIN;
    charMaxUnsigned = UCHAR_MAX;
    charMinUnsigned = UCHAR_MIN;



    

    return 0;
}
